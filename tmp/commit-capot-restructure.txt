docs(readme): alléger 'Ce qu'Eldoria a sous le capot' via asset carres-savoir

La section '🗡️ Ce qu'Eldoria a sous le capot' embarquait 6 cartes SVG
320×200 (≈250 lignes d'inline SVG) sous le Sceau central — sur GitHub,
cette masse se densifieait à l'écran (« bcp de texte en dessous »).

Restructure atomique :
1. Extraction des 6 cartes en un asset autonome
   `public/banner/carres-savoir.svg` (992×422, animations SMIL préservées).
   L'asset reproduit fidèlement les 6 piliers — Monde, Arsenal, Combat,
   Butin, Prouesse, Scène — avec `capGold`, `csBack`, `csDark`,
   `cs3Fire`, `cs4Loot` définis localement (plus de dépendance sur
   l'ID externe capGold du cinebar d'origine).
2. Remplacement dans le README du bloc `<table>` + 6 duplications
   `<svg viewBox="0 0 320 200">` par **un seul** `<img
   src="public/banner/carres-savoir.svg">` cliquable.
3. L'italique de clôture est condensé (les noms des 6 piliers sont
   déjà listés dans l'image — plus besoin de les énumérer en prose).

Bénéfice : -15 026 octets sur README.md, 0 inline SVG 320×200
restant, 1 référence asset nette. Le Sceau et le cinebar d'ouverture
restent intacts.
